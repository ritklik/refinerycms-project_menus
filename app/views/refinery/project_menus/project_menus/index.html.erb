<% content_for :body do %>
  <ul id="project_menus">
    <% @project_menus.each do |project_menu| %>
      <li>
        <%= link_to project_menu.name, refinery.project_menus_project_menu_path(project_menu) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
