<%= render 'refinery/admin/search_header', :url => refinery.project_menus_admin_project_menus_path %>
<div class='pagination_container'>
  <% if @project_menus.any? %>
    <%= render 'project_menus' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
